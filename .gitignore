# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# local database (contains real user posts / resume data)
*.db
*.db-journal
*.sqlite

# generated user content (images with client names — not source)
/public/generated/

# local test scripts that contain live API keys
test-styles.mjs

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# db side-files & stray archives / samples
*.db
*.db-wal
*.db-shm
*.sqlite
*.zip
style-samples/

# keep the example env (no secrets) so the project is runnable
!.env.example

# local render previews (regenerate with the scripts in scripts/)
/public/swipe-preview/
/public/attention-preview/
/public/banner-preview/
/public/assets-preview/
/public/deck-preview/
/public/koyopo-preview/
/public/swipe-file.html
/public/gemini-edit-test.png

# dev server logs
dev.log
dev.err.log

# third-party course transcript — used locally as outreach knowledge, not ours to publish
/src/lib/knowledge/relationship-building.md
